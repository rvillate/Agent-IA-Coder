{
  "name": "agent-coder-remote-runner",
  "version": "1.0.0",
  "description": "Runner remoto para Agent Coder Central. Se conecta a un gateway central y ejecuta jobs dentro de un workspace local seguro.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "generate:key": "node scripts/generate-key.js"
  },
  "dependencies": {
    "playwright": "^1.55.0"
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
