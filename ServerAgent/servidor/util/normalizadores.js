import { env } from '../config/env.js'
export const TIPOS_JOB_VALIDOS = new Set(['shell.exec','file.list','file.read','file.write','file.delete','file.mkdir','file.search','git.status','git.diff','browser.open','browser.click','browser.type','browser.drag','browser.screenshot','browser.eval','browser.close'])
export function normalizarWorkspaceRoots(workspaceRoot, workspaceRoots){ const roots=[]; if(Array.isArray(workspaceRoots)) roots.push(...workspaceRoots); else if(typeof workspaceRoots==='string'&&workspaceRoots.trim()) roots.push(...workspaceRoots.split(/[|,;]/)); if(workspaceRoot) roots.unshift(workspaceRoot); const seen=new Set(); return roots.map(x=>String(x||'').trim()).filter(Boolean).filter(x=>{const k=x.toLowerCase(); if(seen.has(k)) return false; seen.add(k); return true}) }
export const normalizarTrabajosActivos = (items) => Array.isArray(items) ? items.map(x=>String(x||'').trim()).filter(Boolean) : []
export function runnerPublico(row){ if(!row) return null; const lastSeen=row.ultima_vez?new Date(row.ultima_vez).getTime():null; const ageMs=lastSeen?Date.now()-lastSeen:null; return { id:row.id, status:ageMs!=null&&ageMs<env.runnerOfflineMs?'online':'offline', lastStatus:row.estado||row.ultimo_estado||'unknown', lastSeen, lastSeenAgeMs:ageMs, workspaceRoot:row.workspace_root, workspaceRoots:row.workspace_roots||[], platform:row.plataforma, hostname:row.hostname, version:row.version, maxConcurrentJobs:row.max_concurrent_jobs||1, activeJobs:row.trabajos_activos||[], capabilities:row.capacidades||[], metrics:row.metricas||{} } }
export function jobPublico(row, incluirPayload=true){ if(!row) return null; return { id:row.id, type:row.tipo, status:row.estado, runnerTarget:row.runner_target, claimedBy:row.claimed_by, exitCode:row.exit_code, summary:row.resumen, error:row.error, createdAt:row.creado_en?new Date(row.creado_en).getTime():null, updatedAt:row.actualizado_en?new Date(row.actualizado_en).getTime():null, startedAt:row.iniciado_en?new Date(row.iniciado_en).getTime():null, finishedAt:row.terminado_en?new Date(row.terminado_en).getTime():null, needsApprovalAt:row.needs_approval_at?new Date(row.needs_approval_at).getTime():null, stdoutTail:row.stdout_tail||'', stderrTail:row.stderr_tail||'', result:row.resultado||null, truncated:Boolean(row.truncado), localLogPath:row.local_log_path||null, ...(incluirPayload?{payload:row.payload||null}:{}) } }
