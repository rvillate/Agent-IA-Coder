import React from 'react'
import { Activity, Cpu, HardDrive, MemoryStick, RefreshCw, Server } from 'lucide-react'
import { Area, AreaChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
import { useTranslation } from 'react-i18next'
import { Tarjeta, IconBox, Estado } from '../componentes/UI.jsx'
const datosCpu = Array.from({ length: 16 }, (_, i) => ({ n: i, v: 20 + Math.round(Math.sin(i / 2) * 12) + Math.round(Math.random() * 18) }))
function Mini({icon,title,value,estado,tipo='green'}){return <Tarjeta className="mini"><IconBox>{icon}</IconBox><div><span>{title}</span><strong>{value}</strong><Estado tipo={tipo}>{estado}</Estado></div></Tarjeta>}
export function Servidor(){const {t}=useTranslation();return <><Tarjeta className="page-head"><IconBox><Server/></IconBox><div><h1>{t('servidor.titulo')}</h1><p>{t('servidor.subtitulo')}</p></div><button><RefreshCw size={18}/>{t('comun.refrescar')}</button></Tarjeta><div className="stats-grid"><Mini icon={<Cpu/>} title={t('servidor.cpu')} value="8 cores" estado={t('servidor.loadEstable')}/><Mini icon={<MemoryStick/>} title={t('servidor.memoria')} value="4.0 GB" estado={t('servidor.disponible')}/><Mini icon={<HardDrive/>} title={t('servidor.disco')} value="15 GB" estado={`32% ${t('servidor.usado')}`} tipo="yellow"/><Mini icon={<Activity/>} title={t('servidor.red')} value={t('servidor.online')} estado={t('servidor.sshActivo')} tipo="purple"/></div><Tarjeta><h2>{t('servidor.actividad')}</h2><div className="chart"><ResponsiveContainer width="100%" height={240}><AreaChart data={datosCpu}><defs><linearGradient id="g" x1="0" y1="0" x2="0" y2="1"><stop offset="5%" stopColor="#2563eb" stopOpacity={.35}/><stop offset="95%" stopColor="#2563eb" stopOpacity={0}/></linearGradient></defs><XAxis dataKey="n"/><YAxis/><Tooltip/><Area type="monotone" dataKey="v" stroke="#2563eb" fill="url(#g)"/></AreaChart></ResponsiveContainer></div></Tarjeta></>}
